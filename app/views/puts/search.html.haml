.in
  .in__main
    = render 'refrigerator'
    .in__main__header
      - if user_signed_in?
        = link_to "ログアウト", destroy_user_session_path, method: :delete, class: "in__main__header__logout", data: { confirm: 'ログアウトしますか？' }
        .in__main__header__new
          = render 'new'
        .in__main__header__daytime
          = render 'time'
        = form_with(url: search_puts_path, local: true, method: :get, class: "search-form") do |form|
          = form.text_field :keyword, placeholder: "検索する", class: "search-input"
          = form.submit "検索", class: "search-btn"

    -# .in__main__center
    -#   %ul.in__main__center__search
    -#     - @puts.each do |put|
    -#       - if put.user_id == current_user.id
    -#         %li.in__main__center__search__data
    -#           =link_to put_path(put.id),class: "in__main__center__search__data__link" do
    -#             = image_tag put.image.url, size: "120x120", class: "in__main__center__search__data__link__image"
    -#             .in__main__center__search__data__link__image__name
    -#               = put.name
    -#             .in__main__center__search__data__link__image__bestby_date
    -#               = put.bestby_date_y
    -#               %span.txtred<> /
    -#               = put.bestby_date_m
    -#               %span.txtred<> /
    -#               = put.bestby_date_d
        